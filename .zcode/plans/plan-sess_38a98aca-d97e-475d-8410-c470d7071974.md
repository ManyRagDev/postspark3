## Fase D — Absorção mínima do High Ticket no pipeline canônico

### Decisões confirmadas
- **Absorção mínima**: portar apenas (1) contexto enriquecido (BrandKit + Persona + context budget) e (2) intent router com 3 ângulos ortogonais como estratégia alternativa.
- **Backlog das descartadas**: workers paralelos por ângulo, `qaEvaluator`, `correctionLoop`, `visualContractValidator`, `finalMapper` — capabilities documentadas em `BACKLOG_HIGH_TICKET_CAPABILITIES.md` para portagem futura se produto decidir.

---

### Etapa 1 — Mover módulos reutilizáveis para o canônico

De `server/ai/highTicket/` → `server/ai/` (saem do namespace HT, viram canônicos):
- `contextLoader.ts` → `server/ai/contextLoader.ts`
- `contextBudget.ts` → `server/ai/contextBudget.ts`
- `intentRouter.ts` → `server/ai/intentRouter.ts` (+ `angleToStrategy` movido de `highTicket/captionSynthesis.ts:6` para aqui)
- `slimBriefing.ts` → `server/ai/slimBriefing.ts`

Tipos de `shared/highTicket.ts` (`MasterBriefing`, `AngleAssignment`, `RouterOutput`) → `shared/contextBriefing.ts` (nome neutro). Sem consumers fora de `highTicket/` (confirmado pelo inventário).

### Etapa 2 — Integrar no `post.generate`
- **2a.** Contexto enriquecido (BrandKit + Persona) carregado quando persistidos; sem eles, comportamento idêntico ao atual.
- **2b.** Intent router como estratégia alternativa quando `creationMode === "execution" && executionBrief` — `routeHighTicketIntent` produz ângulos → `angleToStrategy` → `ContentStrategy[]` → alimenta o pipeline canônico existente (slots, composition, snapshot, QA, captions).
- **2c.** Remover branch `if (ENV.aiHighTicketPipelineEnabled)` (`routers.ts:645-719`).

### Etapa 3 — Remover pipeline paralelo + backlog
- Remover `server/ai/highTicket/` inteiro (módulos movidos já saíram; 11 restantes são órfãos).
- Remover import + ENV flags `AI_HIGH_TICKET_PIPELINE` / `AI_HIGH_TICKET_LEGACY_FALLBACK`.
- Remover `shared/highTicket.ts`, `shared/highTicketSchemas.ts` + seus testes.
- **Criar `BACKLOG_HIGH_TICKET_CAPABILITIES.md`** documentando as capabilities descartadas (workers paralelos, QA própria, correction loop, visual contract validator, final mapper) com a justificativa (canônico já tem equivalentes) e critérios para portagem futura.

### Etapa 4 — Testes
- Testes dos módulos relocados: contextLoader (fallback limpo), contextBudget (compressão < 18k), intentRouter (angleToStrategy + fallback).
- Teste do branch `creationMode === "execution"` no fluxo canônico.

### Etapa 5 — Documentação
- `DOCUMENTO_MESTRE.md` §73: High Ticket virou modo "execution" do canônico; contexto enriquecido e intent router são módulos canônicos; flags HT removidas; backlog criado.

### Validação
- `pnpm check` + `pnpm test` verdes.
- Teste manual: modo `ideation` (unchanged) e modo `execution` (intent router ativo).