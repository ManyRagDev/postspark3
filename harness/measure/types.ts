/**
 * Promovido para `shared/typography/types.ts` pela SPEC-001 (server, client
 * e harness usam a mesma interface `Measurer`). Este arquivo é só ponte de
 * compatibilidade para os imports existentes do harness.
 */
export * from "../../shared/typography/types";
