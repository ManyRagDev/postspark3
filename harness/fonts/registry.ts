/**
 * Promovido para `shared/typography/fonts/registry.ts` pela SPEC-001 (o
 * registro de fontes precisa ser lido pelo servidor, não só pelo harness).
 * Este arquivo é só ponte de compatibilidade para os imports existentes.
 */
export * from "../../shared/typography/fonts/registry";
