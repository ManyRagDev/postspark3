# Plano de Ação: Validação do Schema PostSpark

## Objetivo
Formalizar o uso do schema `postspark` no Supabase e garantir que a aplicação aponte para ele.

## Passos
1.  [Supabase] Verificar existência do schema `postspark` (Feito).
2.  [Docs] Atualizar `docs/project-status.md` informando que o schema `postspark` está ativo e validado.
3.  [Codebase] Verificar se o arquivo de configuração do Drizzle (`drizzle.config.ts`) aponta para o schema correto (opcional, apenas leitura).

## Critérios de Aceite
*   Schema `postspark` confirmado.
*   Documentação atualizada.
